{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then exit 1; else echo 'Skipping Vercel preview build for non-main branch'; exit 0; fi",
  "crons": [
    {
      "path": "/api/automations/run",
      "schedule": "0 8 * * *"
    }
  ]
}
